# ホットペッパービューティー新規掲載店舗通知システム

## 1. 目的と概要
このシステムは、ホットペッパービューティーの特定エリアにおけるリラクゼーションサロンの新規掲載店舗を定期的に検知し、指定のWebhookを通じて自動で通知することを目的としています。サイト内の「新規オープン」といった明示的な表記に依存せず、過去に検知した店舗リストとの差分を比較することで、新たに掲載された店舗を確実に捕捉します。

## 2. 機能
1.  **データ取得**: 指定されたホットペッパービューティーの店舗一覧ページから、各店舗の「店名」と「詳細URL」を抽出します。
2.  **重複チェック**: 取得した店舗URLを、`past_shops.txt`に記録されている過去のURLと比較します。
3.  **差分抽出**: 過去データに存在しない「新しい店舗URL」のみを抽出します。
4.  **通知**: 新着店舗が発見された場合のみ、設定されたWebhook URL（Discord, Slack, LINE Notifyなど）へ通知メッセージを送信します。
5.  **データ更新**: 今回検知した最新の店舗URLリストを`past_shops.txt`に上書き保存します。GitHub Actionsで運用する場合、この変更は自動的にリポジトリにコミット＆プッシュされます。

## 3. 技術スタック
-   **言語**: Python 3.x
-   **主要ライブラリ**: `requests`, `BeautifulSoup4`
-   **データ管理**: `past_shops.txt`（テキストファイル）
-   **実行環境**: GitHub Actions

## 4. セットアップ
### 4.1. GitHubリポジトリの準備
1.  新しいGitHubリポジトリを作成します。
2.  作成したリポジトリに、以下のファイルを配置します。
    -   `main.py` (メイン処理スクリプト)
    -   `requirements.txt` (必要なPythonライブラリリスト)
    -   `.github/workflows/main.yml` (GitHub Actions設定ファイル)

### 4.2. GitHub Secretsの設定
GitHub Actionsでスクリプトを実行するために、以下の環境変数をGitHubリポジトリのSecretsに設定する必要があります。

1.  GitHubリポジトリの「Settings」タブに移動します。
2.  左側メニューの「Security」セクションにある「Secrets and variables」->「Actions」をクリックします。
3.  「New repository secret」ボタンをクリックし、以下の2つのシークレットを追加します。
    -   **`TARGET_URL`**: 監視対象とするホットペッパービューティーの店舗一覧ページのURLを設定します。
        例: `https://beauty.hotpepper.jp/relax/svcSA/macAC/salon/` (恵比寿・代官山・中目黒・広尾・麻布・六本木エリアのリラクゼーションサロン一覧)
        ※ご希望のエリアのURLに適宜変更してください。
    -   **`WEBHOOK_URL`**: 新着店舗の通知を送信するためのWebhook URLを設定します。Discord, Slack, LINE NotifyなどのWebhook URLを使用できます。
        -   **Discord**: サーバー設定 -> 連携 -> ウェブフック -> ウェブフックを作成
        -   **Slack**: アプリ管理 -> カスタムインテグレーション -> Incoming WebHooks
        -   **LINE Notify**: [LINE Notify公式サイト](https://notify-bot.line.me/ja/)でトークンを発行

### 4.3. `past_shops.txt`について
`past_shops.txt`は、初回実行時に自動的に作成されます。このファイルには、過去に検知した店舗のURLが記録され、次回の実行時に比較対象として使用されます。このファイルはリポジトリにコミットされるため、GitHub Actionsが自動で更新します。

## 5. 運用
### 5.1. 定期実行
`.github/workflows/main.yml`に設定されているCronスケジュールに基づき、GitHub Actionsが自動で定期実行されます。デフォルトでは、毎日午前9時と午後9時（JST）に実行されるように設定されています。

### 5.2. 手動実行
GitHubリポジトリの「Actions」タブに移動し、`Hot Pepper Beauty New Shop Notifier`ワークフローを選択します。「Run workflow」ボタンをクリックすることで、手動で実行することも可能です。

## 6. 注意事項
-   **サーバー負荷軽減**: スクレイピング時には、`time.sleep(1)`によりリクエスト間隔を1秒空けています。また、適切なUser-Agentを設定しています。これらの設定は、対象サイトへの負荷を最小限に抑えるための配慮です。
-   **エラーハンドリング**: ネットワークエラー（ステータスコード200以外）やHTML解析エラーが発生した場合でも、システムがクラッシュしないようにエラーログを出力して安全に終了します。
-   **HTML構造の変更**: ホットペッパービューティーのウェブサイトのHTML構造が変更された場合、`main.py`内の店舗情報を抽出するロジック（`soup.find_all`の部分）の修正が必要になる可能性があります。

---

**開発者**: Manus AI
**最終更新日**: 2026年5月22日
